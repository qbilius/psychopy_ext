# these are default values
# but can be overridden by specific settings in the scripts

# paths to data and analyses


# user-defined global variables
hdr = 2 # Hemodynamic lag in TRs (not seconds)
cmap = default # what plotting colors to use